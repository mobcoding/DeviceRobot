# Android Compose fixture

This directory is reserved for the minimal Jetpack Compose reference application used by navigation, semantics, source evidence, and device execution acceptance tests.

The application itself will be added with the repository intelligence milestone so that its Compose compiler and SDK versions match the managed toolchain selected for that milestone.
