# Android XML fixture

This directory is reserved for the minimal XML View reference application used by repository analysis, Gradle build, permission-dialog, and device execution acceptance tests.

The application itself will be added with the repository intelligence milestone so that its Android Gradle Plugin and SDK versions match the managed toolchain selected for that milestone.
